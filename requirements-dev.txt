# Test/dev tooling. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt -c constraints.txt
pytest>=8.0
pytest-cov>=5.0
reportlab>=4.0        # used by the packet / ZIP segmentation tests
pyflakes>=3.0         # lint
