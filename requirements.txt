pydantic>=2.5
pandas>=2.0
numpy>=1.24        # OCR image preprocessing (Otsu/deskew)
openpyxl>=3.1
PyYAML>=6.0
Flask>=3.0
Flask-Limiter>=4.0    # rate limiting on the web UI's upload/reset routes
pypdf>=4.0
python-docx>=1.1
# OCR for scanned PDFs (optional but recommended). Also needs the Tesseract
# binary — Tesseract 5.x recommended: macOS `brew install tesseract`,
# Ubuntu `apt install tesseract-ocr`.
pypdfium2>=4.0
pytesseract>=0.3.10
pillow>=10.0
# Optional stronger OCR engine for genuinely scanned documents (local/offline,
# no LLM). Install with `pip install rapidocr_onnxruntime`, then select it with
# the env var GRADAPP_OCR_ENGINE=rapidocr. Defaults to Tesseract if unset/missing.
